# Шаблон: Разбор кода / Code Review

## Инструкция

Используй этот шаблон для **анализа кода, паттернов и best practices**.

**Когда использовать:**
- Разбор интересного кода из open source
- Анализ паттерна или антипаттерна
- Best practices для технологии
- Рефакторинг и оптимизация

**Длина:** 300-600 слов

---

## Структура

```
{эмодзи} **{Заголовок: что разбираем}**

{введение: какой код разбираем, почему это интересно}

---

### 📄 Исходный код

```{язык}
{код для разбора}
```

### 🔍 Анализ

{построчный или поблочный разбор}

**Строка X:** {что делает, почему так}

**Строка Y:** {потенциальная проблема, улучшение}

### ✅ Что хорошо

· {positive point 1}
· {positive point 2}

### ⚠️ Что можно улучшить

· {improvement 1}
· {improvement 2}

### 💡 Лучшая практика

```{язык}
{улучшенная версия кода}
```

{объяснение: почему это лучше}

---

### 📚 Применение в проекте

{как использовать эти знания в реальных проектах}

---

🔗 Источники

· [название источника](URL)

#codereview #patterns #теги
```

---

## Пример заполнения

```
🔍 **Property Hooks в PHP 8.4: разбор новой фичи**

PHP 8.4 вводит property hooks — механизм переопределения доступа к свойствам. Разберём, как это работает.

---

📄 Исходный код

```php
class User {
  public string $email {
    get => $this->email;
    set {
      if (!filter_var($value, FILTER_VALIDATE_EMAIL)) {
        throw new InvalidArgumentException('Invalid email');
      }
      $this->email = strtolower(trim($value));
    }
  }
  
  public readonly string $name;
  
  public function __construct(string $email, string $name) {
    $this->email = $email;
    $this->name = $name;
  }
}
```

---

🔍 Анализ

**Property hook `get`:** Возвращает значение без изменений. В данном случае можно было опустить — будет использоваться по умолчанию.

**Property hook `set`:** Валидирует и нормализует email при присваивании. Это заменяет необходимость в отдельном сеттере.

**`readonly $name`:** Свойство только для чтения, устанавливается в конструкторе.

---

✅ Что хорошо

· Валидация инкапсулирована в свойстве
· Нет необходимости в отдельных методах
· Код становится декларативнее

⚠️ Что можно улучшить

· В `get` можно добавить форматирование (например, для отображения)
· Рассмотреть использование Value Objects для email

---

💡 Лучшая практика

Для сложной логики выноси в отдельные методы:

```php
class User {
  public string $email {
    get => $this->email;
    set => $this->email = $this->normalizeEmail($value);
  }
  
  private function normalizeEmail(string $email): string {
    if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
      throw new InvalidArgumentException('Invalid email');
    }
    return strtolower(trim($email));
  }
}
```

**Почему лучше:**
- Логика валидации тестируется отдельно
- Метод можно переиспользовать
- Чёткое разделение ответственности

---

📚 Применение в проекте

Используй property hooks для:
- Валидации данных при присваивании
- Нормализации значений (trim, strtolower)
- Логирования доступа к свойствам
- Вычисляемых свойств без геттеров

---

🔗 Источники

· [PHP 8.4 RFC: Property Hooks](https://wiki.php.net/rfc/property-hooks)
· [PHP.net Documentation](https://www.php.net/manual/ru/language.oop5.property-hooks.php)

#codereview #php #patterns
```

---

## Советы по написанию

### Выбор кода

- Бери реальные примеры из open source
- Или создавай реалистичные кейсы
- Избегай слишком тривиальных примеров

### Анализ

- Будь конструктивен, не критикуй ради критики
- Объясняй **почему**, а не только **что**
- Показывай альтернативы

### Баланс

- 2-3 положительных момента
- 2-3 момента для улучшения
- 1 улучшенная версия кода

---

## Чек-лист перед использованием

- [ ] Код в блоке с правильным языком
- [ ] Есть построчный/поблочный анализ
- [ ] Указаны положительные стороны
- [ ] Указаны моменты для улучшения
- [ ] Показана улучшенная версия
- [ ] Есть раздел о применении в проекте
- [ ] Источники указаны как кликабельные ссылки [название](URL)
